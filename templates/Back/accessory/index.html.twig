{% extends 'Back/base.html.twig' %}

{% block title %}Accessory index{% endblock %}

{% block body %}
    <div class="w-5/6 h-auto mx-auto px-8 py-16 bg-grey-lighter">
        <div class="flex justify-between items-center">
            <h1 class="text-4xl text-primary font-bold inline-block">Liste des accessoires</h1>
            <button class="btn btn-secondary"><a href="{{ path('accessory_new') }}">Ajouter</a></button>
        </div>
        <table class="table-auto border-collapse w-full mt-12 rounded bg-white" cellpadding="0" cellspacing="0">
            <thead class="font-bold bg-blue-darker text-white">
            <tr>
                <td>Id</td>
                <td>Nom</td>
                <td>Modèle</td>
                <td>Image</td>
                <td>Editer</td>
                <td>Supprimer</td>
            </tr>
            </thead>
            <tbody>
            {% for accessory in accessories %}
                <tr>
                    <td>{{ accessory.id }}</td>
                    <td>{{ accessory.name }}</td>
                    <td>{{ accessory.model }}</td>
                    <td> a faire</td>
                    <td><a href="{{ path('accessory_edit', {'id' : accessory.id}) }}" class="font-bold text-primary">Editer</a></td>
                    <td>{{ include('Back/accessory/_delete_form.html.twig') }}</td>
                </tr>
            {% endfor %}

            </tbody>
        </table>
    </div>
{% endblock %}

{% extends 'Back/base.html.twig' %}

{% block title %}Accessory{% endblock %}

{% block body %}
    <h1>Accessoires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accessory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ accessory.name }}</td>
            </tr>
            <tr>
                <th>ImgName</th>
                <td>{{ accessory.imgName }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ accessory.updatedAt ? accessory.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ accessory.model }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('accessory_index') }}">back to list</a>

    <a href="{{ path('accessory_edit', {'id': accessory.id}) }}">edit</a>

    {{ include('Back/accessory/_delete_form.html.twig') }}
{% endblock %}
